using System;
using System.Collections.Generic;
using System.Text;

namespace EventPassMX_programacion
{
    // This class represented the old Dashboard UI placeholder. Kept for compatibility but not used.
    // The main window for features is `DashboardWindow`.
    internal static class Dashboard
    {
        // provide a simple helper to open the features - return null to signal no-op
        public static void Open(string usuario)
        {
            // no-op helper kept for compatibility
        }
    }
}
